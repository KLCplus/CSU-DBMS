# CSUDB 文件结构规划（函数级）

本文档描述**本人负责的全部功能**：① SQL 编译器（`src/observer/sql/parser/`）、② SQL 输入补全（`src/observer/sql/autocomplete/`）、③ 命令行客户端与 Web Console（`src/obclient/`）、④ SDK 与 JDBC（`sdk/python/`、`sdk/java/`）、⑤ 公网接入与发行（`scripts/`、`gh-pages` 分支）。并细化到**每个文件中的关键类与函数**。查询处理/执行与存储等层级不属于本人工作范围，仅在分层表中标注，不展开文件明细。

- 路径约定：第 3 节编译器/补全未写前缀的路径相对于 `src/observer/sql/`；第 6~8 节使用仓库相对路径（`src/obclient/`、`sdk/`、`scripts/`）。
- 生成产物：`parser/lex_sql.cpp/.h` 与 `parser/yacc_sql.cpp/.hpp` 由 Flex/Bison 自动生成，不作为函数级说明对象；其源文件为 `parser/lex_sql.l` 与 `parser/yacc_sql.y`。

---

## 0. 范围与系统分层

本项目按「编译原理 × 数据库系统 × 操作系统」三层协作。**本文档覆盖本人负责的 ① 编译器、② 补全、③ 客户端/Web、④ SDK、⑤ 公网接入与发行**；队友负责的查询执行与存储仅在分层表中标注，不展开文件明细。

| 层 | 目录 | 职责 | 归属 |
| --- | --- | --- | --- |
| ① SQL 编译器层 | `sql/parser/`（`lex_sql.l`、`yacc_sql.y`、`parse.*`、`parse_stage`、`resolve_stage`、`expression_binder`） | 词法/语法/语义分析，产出 AST 与语句对象 | **本人** |
| ② SQL 输入补全层 | `sql/autocomplete/` | 确定性补全（Parser 期望集合 + Catalog + 方言门禁）+ 可选 FIM 模型 ghost | **本人** |
| ③ 客户端与 Web 层 | `src/obclient/`（`client.cpp`、`web_console_launcher.cpp`、`web/`） | 命令行客户端、Web Console 网关与前端 | **本人** |
| ④ SDK 层 | `sdk/python/`、`sdk/java/` | Python DB-API 驱动、JDBC 4 驱动 | **本人** |
| ⑤ 公网接入与发行 | `scripts/`（打包/代理/模型脚本）、`gh-pages` 分支 | 协议嗅探代理、隧道接入、装包发行 | **本人** |
| ⑥ 查询处理 / 执行层（数据库系统） | `sql/stmt/`、`sql/expr/`、`sql/optimizer/`、`sql/operator/`、`sql/executor/` | AST → 语句对象 → 逻辑计划 → 优化 → 物理计划 → 执行算子 | 队友 |
| ⑦ 存储引擎层（操作系统 / 存储） | `storage/` | 表 / 记录 / 页 / Buffer Pool / 索引 / 事务 / 日志 | 队友 |
| ⑧ 通信 / 服务 / 会话层 | `net/`、`service/`、`session/`、`event/` | 协议、鉴权、会话、结果下发 | 队友（本人补全协议在其上扩展） |

边界说明：

- 编译器层到**语义分析、产生 AST 与语句对象（`stmt/*`）为止**；`sql/stmt/` 是语义分析的产物、也是查询处理的输入，项目分工上归**查询处理层（队友）**。
- 补全 / 客户端 / Web / SDK / 发行均为本人在编译器之外的实现；其中补全的**服务端协议入口**（`event/`、`net/`、`service/`）在其通信层之上扩展。
- 优化、算子、执行、存储读写属编译器**下游**，不在本文档与本人实现范围内。
- 项目基于 OceanBase Miniob 改造，`LICENSE` / `NOTICE` 保留上游版权说明。

---

## 1. 总览

编译器层是流水线的**前端**：词法分析 → 语法分析 → 语义解析（名字/表达式绑定）→ 产出 AST / 语句对象。

### 1.1 文件夹级结构

```
src/observer/sql
├── parser/       词法分析、语法分析、语义解析（名字绑定）
└── autocomplete/ SQL 输入补全（确定性 Parser+Catalog，可选 FIM 模型）
```

### 1.2 文件级结构树

> 未标后缀的单独条目即该文件名；`a.h / a.cpp` 表示一对同名头/源文件。

**parser/ —— 词法 / 语法 / 语义解析**

```
parser/
├── parse.h / parse.cpp                    语法解析总入口
├── parse_defs.h                           语法树中间节点、比较运算符与命令标志枚举
├── lex_sql.l                              词法规则源文件（交由 Flex 生成 lex_sql.cpp/.h）
├── yacc_sql.y                             语法与优先级源文件（交由 Bison 生成 yacc_sql.cpp/.hpp）
├── expected_tokens.h                      光标处期望 token 集合接口（补全与语法诊断复用）
├── expression_binder.h / expression_binder.cpp   表达式与名字绑定器（表/列存在性、类型校验）
├── parse_stage.h / parse_stage.cpp        语法解析阶段
└── resolve_stage.h / resolve_stage.cpp    语义解析阶段（名字绑定、语义错误透出）
```

**autocomplete/ —— SQL 输入补全**

```
autocomplete/
├── README.md                                      模块说明
├── completion_types.h / .cpp                      补全请求、响应与候选数据结构
├── current_statement_extractor.h / .cpp           取光标所在单条语句（分号/注释/字符串安全）
├── sql_text_scanner.h / .cpp                      轻量 SQL 文本扫描器（非语法分析器）
├── sql_capabilities.h / .cpp                      运行时 SQL 能力表（方言门禁）
├── grammar_completion_provider.h / .cpp           基于语法分析期望集合的关键字补全
├── catalog_completion_provider.h / .cpp           基于目录的表与列补全
├── completion_scope.h / .cpp                      补全作用域（来源/JOIN/插入目标）扫描
├── completion_engine.h / .cpp                     补全统一入口
├── sql_completion_config.h / .cpp                 补全配置加载
├── llama_completion_client.h / .cpp               模型填充接口客户端（带超时）
├── model_context_builder.h / .cpp                 模型上下文（受限方言与相关表结构）
├── model_completion_provider.h / .cpp             模型补全服务（健康检查、调用、降级）
└── model_completion_validator.h / .cpp            模型输出校验与最长合法前缀裁剪
```

### 1.3 编译数据流

```
SQL 文本
  → parser/lex_sql.l                          词法分析（token + 行列位置）
  → parser/yacc_sql.y                         语法分析 → parse_defs.h 的 AST
  → parser/resolve_stage + expression_binder  语义分析 / 名字绑定 → Stmt
（之后：逻辑计划 → 优化 → 物理计划 → 执行 → 存储，由队友负责，不在本文档范围）
```

---

## 2. 编译器 Stage 编排

| 阶段 | 实现（文件） | 关键函数 | 输入 → 输出 |
| --- | --- | --- | --- |
| ParseStage | `parser/parse_stage.*` | `ParseStage::handle_request` | SQL 字符串 → `ParsedSqlResult`（AST） |
| ResolveStage | `parser/resolve_stage.*` | `ResolveStage::handle_request` | AST → `Stmt *`（名字/表达式绑定） |

> `OptimizeStage`、`ExecuteStage` 属于下游（查询处理/执行层），由队友负责。

---

## 3. 编译器目录函数级明细

### 3.1 parser —— 词法 / 语法 / 语义解析

#### 3.1.1 解析入口与 AST 定义

| 文件 | 类/函数 | 说明 |
| --- | --- | --- |
| `parse.h` / `parse.cpp` | `parse()` | 语法解析总入口（`ParsedSqlResult::add_sql_node` 收集节点） |
| `parse_defs.h` | 各 AST 中间节点 | 见下方 |

`parse_defs.h` 定义的节点与枚举：

- **结构体（AST 节点）**：`RelAttrSqlNode`（表.列）、`ConditionSqlNode`（比较条件）、`JoinSqlNode`（显式 JOIN）、`SelectSqlNode`、`InsertSqlNode`、`DeleteSqlNode`、`UpdateSqlNode`、`AttrInfoSqlNode`（含 `nullable`）、`CreateTableSqlNode`、`DropTableSqlNode`、`AnalyzeTableSqlNode`、`CreateIndexSqlNode`、`DropIndexSqlNode`、`DescTableSqlNode`、`LoadDataSqlNode`、`SetVariableSqlNode`、`ExplainSqlNode`、`ErrorSqlNode`（`error_msg/line/column`）、`CalcSqlNode`。
- **枚举**：`CompOp`（`EQUAL_TO/LESS_EQUAL/NOT_EQUAL/LESS_THAN/GREAT_EQUAL/GREAT_THAN/IS_NULL/IS_NOT_NULL/NO_OP`）、`SqlCommandFlag`（`SCF_*`）。
- **类**：
  - `ParsedSqlNode`：`ParsedSqlNode()`（默认构造）、`ParsedSqlNode(SqlCommandFlag)`；承载 `flag` 与各语句节点联合体。
  - `ParsedSqlResult`：`add_sql_node()`（追加解析结果）、`sql_nodes()`（访问器）。

#### 3.1.2 词法/语法生成源

| 文件 | 关键点 |
| --- | --- |
| `lex_sql.l` | Flex 源：关键字/标识符/常量/运算符规则（含 `OR`）、`%x COMMENT` 多行注释状态、字符串转义（`''`/`""`/`\x`）、`%option yylineno` + `YY_USER_INIT`/`YY_USER_ACTION` 输出 **1-based 跨行行列**、`scan_string()` 绑定输入。生成 `lex_sql.cpp/.h`（`yylex` 等）。 |
| `yacc_sql.y` | Bison 源：文法与优先级（`%left/%precedence`，`OR < AND < 比较 < NOT`）、`%define parse.error custom` + `yyreport_syntax_error()`（位置 + unexpected + expected）、`unescape_quoted_string()`、`create_arithmetic_expression()`、`create_aggregate_expression()`、`set_expr_name_and_location()`、`append_join_node()`、`collect_expected_tokens()`（补全复用期望集合）。生成 `yacc_sql.cpp/.hpp`（`yyparse`）。 |
| `expected_tokens.h` | `collect_expected_tokens()` 声明：在 SQL 前缀末尾追加哨兵，复用 bison 期望集合，供自动补全使用。 |

#### 3.1.3 语义解析（名字/表达式绑定）

| 文件 | 类 | 关键函数 |
| --- | --- | --- |
| `expression_binder.h` / `expression_binder.cpp` | `ExpressionBinder`、`BinderContext` | `bind_expression`（分发入口）、`bind_star_expression`（`*` 展开）、`bind_unbound_field_expression`（字段→`FieldExpr` + 表/列存在性校验）、`bind_field_expression`、`bind_value_expression`、`bind_cast_expression`（类型转换）、`bind_comparison_expression`（含 `IS [NOT] NULL`）、`bind_conjunction_expression`（AND/OR）、`bind_arithmetic_expression`（算术 + 类型检查）、`bind_aggregate_expression`（聚合绑定）；`BinderContext::add_table`、`reset_binder_error_message`/`set_binder_error_message` |

#### 3.1.4 Stage

| 文件 | 类 | 关键函数 |
| --- | --- | --- |
| `parse_stage.h` / `.cpp` | `ParseStage` | `handle_request` |
| `resolve_stage.h` / `.cpp` | `ResolveStage` | `handle_request` |

---

### 3.2 autocomplete —— SQL 输入补全

| 文件 | 类 / 函数 | 说明 |
| --- | --- | --- |
| `completion_types.h` / `.cpp` | `CompletionItem`、`CompletionKind`、`CompletionSource`、`CompletionRequest/Response`；`completion_kind_name/source_name` | 与 UI 无关的补全数据结构 |
| `current_statement_extractor.h` / `.cpp` | `find_statement_at_cursor()` | 取光标所在 statement（分号/注释/字符串安全） |
| `sql_text_scanner.h` / `.cpp` | `scan_sql_text()`、`cursor_in_string_or_comment()` | 轻量文本切分（非语法分析器） |
| `sql_capabilities.h` / `.cpp` | `SqlCapabilities::instance/is_keyword/is_type/is_forbidden` | 运行时 SQL 能力表（方言门禁） |
| `grammar_completion_provider.h` / `.cpp` | `complete_grammar()` | 基于 Parser 期望集合的关键字/运算符/类型补全 |
| `catalog_completion_provider.h` / `.cpp` | `complete_catalog()` | 基于真实 Catalog 的表/列补全（含 `INSERT INTO t(`） |
| `completion_scope.h` / `.cpp` | `build_completion_scope()`、`CompletionScope` | FROM/JOIN/INSERT INTO 作用域扫描 |
| `completion_engine.h` / `.cpp` | `CompletionEngine::complete()` | 统一入口；组合确定性补全与可选模型补全 |
| `sql_completion_config.h` / `.cpp` | `SqlCompletionConfig::load()` | 配置加载（无 magic number） |
| `llama_completion_client.h` / `.cpp` | `LlamaCompletionClient::health/infill` | llama.cpp `/infill` HTTP 客户端（带 deadline） |
| `model_context_builder.h` / `.cpp` | `SqlModelContextBuilder::build()` | 生成受约束的 dialect + 相关 schema 上下文 |
| `model_completion_provider.h` / `.cpp` | `ModelCompletionProvider::available/complete()` | 模型 provider（健康检查 + 调用 + 校验 + 降级） |
| `model_completion_validator.h` / `.cpp` | `ModelCompletionValidator::validate()` | 清洗 → 方言截断 → Parser 校验 → Catalog 校验 → 最长合法前缀 |

对上/对外集成（不在 `sql/autocomplete/` 目录内，属编译器能力的对外接口）：

| 文件 | 类 / 函数 | 说明 |
| --- | --- | --- |
| `sql/parser/expected_tokens.h` + `parser/yacc_sql.y` | `collect_expected_tokens()` / `yyreport_syntax_error()` | 复用 bison 期望集合，供补全与语法诊断共用 |
| `event/session_event.h` | `ClientRequestType::COMPLETE`、`set/get_completion_*` | 补全请求类型与字段 |
| `net/native_communicator.cpp` | `read_event`（`type == "complete"`） | native 协议解析补全请求 |
| `service/database_service.{h,cpp}` | `DatabaseService::complete_sql()`、`completion_engine()` | 进程级 `CompletionEngine` 单例 + 响应编码 |
| `obclient/client.cpp` | `fetch_completion`、`complete_line`、`hint_line`、`print_completion` | CLI：Tab 补全、ghost text、`--complete`、`/complete` |
| `obclient/web/csudb_web.py` | `POST /api/complete` | Web Gateway 转发补全请求（会话经 `X-CSUDB-Session`/Cookie） |
| `obclient/web/app.js` | `acRequest/acRender/acAccept/acShowGhost` | Web：候选下拉 + AI ghost（Tab/Enter/点击接受） |
| `obclient/web/index.html` / `app.css` | `editor-wrap`、`sql-complete`、`sql-ghost` | Web 补全 UI 结构与样式 |
| `etc/sql_completion.json` | — | 补全配置（模型开关/地址/超时/上下文预算） |
| `scripts/build_llama_p40.sh` / `run_sql_completion_model.sh` | — | P40 CUDA12 构建与模型启动 |

---

## 4. 编译器需求到代码落点映射

| 需求项 | 主要落点（文件:函数） |
| --- | --- |
| 词法分析（关键字/常量/运算符/注释/转义/位置） | `parser/lex_sql.l`（规则段 + `YY_USER_ACTION`） |
| 语法分析（优先级、括号、AST） | `parser/yacc_sql.y`、`parser/parse_defs.h` |
| 语义分析（表/列存在性、名字绑定、类型一致性） | `parser/expression_binder.*::bind_*`、`parser/resolve_stage.*::handle_request` |
| 语法诊断 / 期望集合 | `parser/yacc_sql.y::yyreport_syntax_error`、`parser/expected_tokens.h::collect_expected_tokens`、`parser/parse_stage.cpp::ParseStage::handle_request` |
| 字符串转义 | `parser/lex_sql.l`（字符串规则）、`parser/yacc_sql.y::unescape_quoted_string` |
| 语义错误定位 | `parser/expression_binder.cpp`（消息槽 + `set_arithmetic_type_error`）、`parser/resolve_stage.cpp` |
| SQL 输入补全 | `autocomplete/*`、`net/native_communicator.cpp`、`service/database_service.cpp::complete_sql`、`obclient/client.cpp` |

---

## 5. 配套测试与脚本

| 位置 | 说明 |
| --- | --- |
| `unittest/observer/autocomplete_test.cpp` | 补全单元测试：扫描器、语句提取、能力表、Parser 期望集合、grammar 补全、engine 关键字/大小写/门禁/注释字符串/多语句、模型输出校验。 |
| `SQL_description/test/run_tests.py` | SQL 编译器测试集运行器：自动拉起临时 `csudbd` 或连接已有服务端，输出 `PASS/FAIL/CRASH/WRONG_ACCEPT/WRONG_REJECT/WRONG_LOCATION/WRONG_RESULT`。 |
| `SQL_description/test/cases/` | 用例：`lexer.py`（词法）、`parser.py`（语法）、`semantic.py`（语义）、`autocomplete.py`（补全）；另含 `core_sql.py`、`boundary.py` 作为端到端验证。 |
| `etc/sql_completion.json` | 补全配置（开关、模型地址/超时、上下文预算等）。 |
| `scripts/build_llama_p40.sh` / `scripts/run_sql_completion_model.sh` | P40（CUDA 12.x / sm_61 / Q8_0）构建与启动 llama.cpp `/infill` 模型 sidecar；`run_...` 支持本地 GGUF（`SQL_COMPLETION_MODEL_FILE`）。 |
| `obclient/web/{csudb_web.py,app.js,app.css,index.html}` | Web Console 补全：`/api/complete` + 候选下拉 + AI ghost。 |

---

## 6. 客户端与 Web Console（`src/obclient/`）

### 6.1 命令行客户端 `client.cpp`

| 类 / 函数 | 说明 |
| --- | --- |
| `ClientOptions`、`CliOverrides` | 连接、输出与命令行覆盖选项 |
| `apply_section` / `apply_environment` / `apply_url` | 配置文件、环境变量、`--url` 解析（`host[:port]`，可本地/公网，剥离 `xxx://` 前缀与路径） |
| `parse_options` | 参数解析与优先级合并（默认 < 配置文件 < 环境变量 < 命令行） |
| `NativeConnection::connect_to` / `request` | native 协议连接与 `JSON + '\0'` 收发（不解析 SQL） |
| `fetch_completion` / `complete_line` / `hint_line` / `print_completion` | 补全：native `complete` 请求、Tab 候选、ghost hint、`--complete` / `/complete` 输出 |
| `Shell`（`/help` `/status` `/use` `/timing` `/buffer` `/pages` `/web` `/complete` `/q`） | 交互命令分发与结果渲染 |
| `main` / `banner` / `read_password` | 启动、登录横幅、无回显密码读取 |

### 6.2 Web Console（`src/obclient/web/`）

| 文件 | 类 / 函数 / 路由 | 说明 |
| --- | --- | --- |
| `csudb_web.py` | `SessionStore`（`create/get/remove/close_all`） | 内存 `token → Connection` 会话（无 TTL，靠 logout/退出释放） |
| | `Handler.do_GET/do_POST` 的 `/api/connect`、`/api/logout`、`/api/query`、`/api/use`、`/api/complete`、`/api/status`、`/api/pages`、`/api/databases`、`/api/tables`、`/api/table`、`/api/schema`、`/api/health` | HTTP 网关；查询/补全经 Python SDK 走 native；使用 `X-CSUDB-Session`（优先）或 Cookie 鉴权 |
| | `_security_headers`、`do_OPTIONS` | CORS（回显 Origin、允许 `X-CSUDB-Session`）与安全响应头 |
| | `parse_arguments`、`main` | 启动参数与**仅回环**监听校验 |
| `index.html` / `app.js` / `app.css` | `api/post/text/num/table/run/refresh/...`、`acRequest`/`acRender`/`acAccept`/`acShowGhost` | 控制台前端：状态/表数据/SQL 工作区；补全下拉 + AI ghost（Tab/Enter/点击接受）；`window.CSUDB_API_BASE` 拼接请求地址 |

### 6.3 本地 Web 启动器 `web_console_launcher.{h,cpp}`

| 函数 | 说明 |
| --- | --- |
| `state_directory/pid_path/log_path` | `~/.csudb` 状态目录、pid 与日志路径 |
| `read_pid/is_web_process` | 读取并校验残留 pid（用 `/proc/<pid>/cmdline` 双重确认，避免误杀） |
| `find_web_script/executable_path` | 定位 `csudb-web.py` 与可执行文件 |
| `start_web_console/stop_web_console/web_console_status` | `/web` 的启动、停止与状态查询（`fork+setsid`，日志落 `~/.csudb/web.log`） |

---

## 7. SDK（`sdk/python/`、`sdk/java/`）

### 7.1 Python 驱动 `sdk/python/csudb.py`

| 类 / 函数 | 说明 |
| --- | --- |
| `connect(host, port, user, password, database, timeout)` | 建立并登录 native 会话；未知参数报 `TypeError`，端口非法报 `InterfaceError` |
| `_NativeConnection.__init__/close/request` | socket 生命周期、`UTF-8 JSON + '\0'` framing、16 MiB 上限、错误分类 |
| `_quote` / `_bind` | `%s` 占位符数量校验与**客户端字面量转义**（非服务端 prepared） |
| `Connection`（`cursor/execute/commit/rollback/server_info/buffer_snapshot/close`、上下文管理） | 会话对象与诊断扩展 |
| `Cursor`（`execute/executemany/fetchone/fetchmany/fetchall/description/rowcount/迭代/close`） | 物化结果游标 |
| 异常层级 `Warning/Error/InterfaceError/DatabaseError(DataError/OperationalError/IntegrityError/InternalError/ProgrammingError/NotSupportedError)` | 对齐 DB-API 2.0 |
| `sdk/python/README.md` | 接口文档 |

### 7.2 JDBC 驱动 `sdk/java/src/main/java/edu/csu/csudb/jdbc/`

| 文件 | 类 / 关键点 | 说明 |
| --- | --- | --- |
| `CsuDbDriver.java` | `CsuDbDriver`（静态注册 + `acceptsURL/getPropertyInfo/connect`）、`META-INF/services` | JDBC 4 自动发现；`jdbc:csudb://HOST:PORT/DATABASE`，属性 `user/password/connectTimeout` |
| `NativeClient.java` | `NativeClient`（构造登录、`query/ping/close`、`request/checked`） | native 传输层；错误码写入 `SQLException` |
| `JdbcProxies.java` | `ConnectionHandler` / `StatementHandler` / `ResultSetHandler`、`resultSetMetaData`、`databaseMetaData`、`bind`/`literal`/`quote` | 动态代理实现 JDBC 子集；未实现统一 `SQLFeatureNotSupportedException`；`?` 客户端转义 |
| `Json.java` | `stringify/parseObject`（递归下降 `Parser`） | 自实现 JSON 编解码，零第三方依赖 |
| `sdk/java/README.md` | 接口文档 | 构建、URL、支持子集与限制 |

---

## 8. 公网接入与发行（`scripts/`、`gh-pages`）

### 8.1 协议嗅探代理 `scripts/tcp_proxy.py`

| 函数 | 说明 |
| --- | --- |
| `handle` | 对每个连接 `recv(8, MSG_PEEK)` 只窥探首批字节，按 HTTP 方法前缀（`GET/POST/PUT/HEAD/OPTI/DELE/CONN/PATC`）判定协议 |
| `pipe` | 双向全双工转发（`recv(65536)`/`sendall`，异常安全关闭） |
| `main` | 监听 `0.0.0.0:8157`；HTTP → `127.0.0.1:8765`，native → `127.0.0.1:6789` |

### 8.2 打包与发行脚本

| 文件 | 说明 |
| --- | --- |
| `scripts/package_sdk.sh` | 打包 Python SDK（tar.gz）与 JDBC（jar），产出 `SDK-README.md` 与 `sha256` |
| `scripts/package_client.sh` | Release + `STATIC_STDLIB=ON` 构建 CLI，打包 `csudb-client-linux-x64.tar.gz(.sha256)` |
| `scripts/sdk_readme.md` / `scripts/client_readme.md` | 随包说明（公网地址、用法、选项速查） |
| `scripts/completion/csudb.bash` | Bash Tab 补全脚本 |
| `gh-pages` 分支 | GitHub Pages 静态前端 + `config.js`（`window.CSUDB_API_BASE` 指向隧道地址） |

### 8.3 模型 sidecar 脚本

| 文件 | 说明 |
| --- | --- |
| `scripts/build_llama_p40.sh` | P40（CUDA 12.x / sm_61 / MMQ）构建 llama.cpp |
| `scripts/run_sql_completion_model.sh` | 启动 `llama-server` `/infill`（支持本地 GGUF 或 HuggingFace） |

### 8.4 公网访问架构

```
浏览器 / GitHub Pages ──HTTPS──▶ 隧道 ──▶ :8157 协议嗅探代理 ──HTTP──▶ :8765 Web Gateway ──native──▶ :6789 数据库
CLI / Python SDK / JDBC ──native TCP──▶ :8157 协议嗅探代理 ──native──▶ :6789 数据库
```

- 公网端口 **8157** 同时服务网页与 native 客户端；`8765`（Web）与 `6789`（DB）仅监听回环。
- HTTPS 由隧道侧提供（本地代理为明文 TCP）；隧道地址为临时地址，重启后需同步 `gh-pages/config.js`。
