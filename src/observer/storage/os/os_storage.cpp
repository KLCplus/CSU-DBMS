#include "storage/os/os_storage.h"

// This translation unit keeps the header-only OS facade in the normal build so that
// include relationships are continuously checked without introducing new runtime logic.

